to install golang ->  sudo apt install golang

to run -> go run main.go 
to start a service -> nohup go run main.go &
to list process -> pgrep -f "go run"
to get list of all process -> ps -as
to kill process -> kill process_id

